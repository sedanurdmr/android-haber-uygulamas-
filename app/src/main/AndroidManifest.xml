<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sedanur.haber">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gecis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".haberler"></activity>
        <activity android:name=".detay"></activity>
        <activity android:name=".kullanicigiris"></activity>
        <activity android:name=".uyegiris"></activity>
        <activity android:name=".uyeol"></activity>
        <activity android:name=".uyehaber"></activity>
        <activity android:name=".haberdetay"></activity>

    </application>

</manifest>